{
  "name": "jps.wasm",
  "packageManager": "yarn@4.5.1",
  "scripts": {
    "prepare": "cd build && rm -rf * && emcmake cmake ../src",
    "build:cmake": "cmake --build ./build",
    "build": "yarn prepare && yarn build:cmake && yarn examples",
    "examples": "cp build/jps.* examples/minimal-example"
  }
}
